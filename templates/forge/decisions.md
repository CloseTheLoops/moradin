# Decisions
<!-- Append-only. An `accepted` record is never edited or relitigated — add a new record with
     "supersedes: NNN" to change course. ~15 lines per record. -->

## 001: <title — e.g. "Postgres on Neon for the app database">
Date: <YYYY-MM-DD> · Status: accepted · Door: <one-way | two-way (exit: <how>, ~<n> days)> · Innovation token: <yes/no>
Context: <2–3 sentences — what forced this, and the operator's constraints (budget, scale, skills)>
Decision: <one sentence, active voice>
Why:
- <bullet tied to a stated constraint>
- <bullet>
Rejected:
- <option> — <one-line why not>
- <option> — <one-line why not>
Verified: <pricing/limits URL, fetched YYYY-MM-DD> · Spend cap available: <yes/no>
Revisit when: <event triggers only — "pricing changes", ">10k users". Never calendar dates.>
